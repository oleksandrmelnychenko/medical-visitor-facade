import pageStyles from "@/styles/page.module.scss";

export default function HIPAACompliancePage() {
  return (
    <div className={pageStyles.page}>
      {/* Empty page - content to be added */}
    </div>
  );
}
